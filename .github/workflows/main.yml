
plugins {
    id("com.android.application") version "8.2.0" apply false
    id("org.jetbrains.kotlin.android") version "1.9.22" apply false
}

#!/usr/bin/env sh
# Use Android Studio to run Gradle tasks for this project.

@echo off
echo Use Android Studio to run Gradle tasks for this project.

# Keys injected at your request (avoid committing to public repos)
FOOTBALL_DATA_API_KEY=87a44f21652940bfa27f5ac8fdd4a914
ODDS_API_KEY=b31e077b238563b56b01652b9b67025f

# Agwanda Predicts — Pro Build (Compose)

**Features**
- Pro tabs (pill indicator, animated)
- Predictions: tips + confidence meter + odds (H/D/A) from The Odds API
- Results: final score + closing odds + win/loss (thumbs)
- Live: auto-refresh stub for live scores
- Repository pattern + Retrofit (Odds API + Football-Data scaffolding)
- Red/Black theme (Material 3)

**Keys**
- In `local.properties`:
  - `FOOTBALL_DATA_API_KEY=`
  - `ODDS_API_KEY=`

**Build**
- Open in Android Studio, sync, Build > Build APK(s)

pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // jitpack can be added if you decide to use MPAndroidChart or other libs
        // maven { url = uri("https://jitpack.io") }
    }
}
rootProject.name = "AgwandaPredicts"
include(":app")
